;; -*- mode: emacs-lisp -*-
;; History entries used for anything adaptive display.
(setq anything-c-adaptive-history (quote ((file ("/home/." ("" . 1))) ("Emacs Commands History" ("shell" ("" . 3))))))
